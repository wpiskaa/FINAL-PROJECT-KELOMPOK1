{
  "name": "brewmate-pos",
  "version": "1.0.0",
  "description": "BrewMate POS — Sistem Kasir Coffee Shop + Gemini AI (Kelompok 1 PAW)",
  "private": true,
  "scripts": {
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev": "node -e \"const { spawn } = require('child_process'); const b = spawn('npm', ['--prefix', 'backend', 'run', 'dev'], { stdio: 'inherit', shell: true }); const f = spawn('npm', ['--prefix', 'frontend', 'run', 'dev'], { stdio: 'inherit', shell: true }); process.on('SIGINT', () => { b.kill(); f.kill(); process.exit(); });\"",
    "build": "npm --prefix frontend run build",
    "test": "npm --prefix backend run test"
  },
  "keywords": [
    "pos",
    "coffee-shop",
    "gemini-ai",
    "react",
    "express",
    "sqlite"
  ],
  "author": "Kelompok 1 - PAW 2026",
  "license": "ISC"
}
